// Goal: Print every word in all lowercase letters.
// Steps: Make an array of strings
// Make a for statement to run through the strings to print all the words in the array
// Use a method to print only lower case 

var nicknames = ["cat daddy", "samari", "Plant daddy", "Dodgeball queen", "coding legend"] ;

for (var i = 0; i < nicknames.length; i ++) {
    console.log(nicknames[i].toLowerCase())
}
