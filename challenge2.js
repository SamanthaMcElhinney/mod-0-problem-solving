// Goal is to print every word in all lowercase letters from an array.
// First make an array with mix of upper and lower case.
// Next make a for statement to iterate over an array.
// Then make an if statement to include lowercase.

var nicknames = ["cat daddy", "samari", "Plant daddy", "Dodgeball queen", "coding legend"] ;

for (var i = 0; i < nicknames.length; i ++) {
    console.log(nicknames[i].length)
}